# 2026-05-20

- memory_type: manual-post-push
  record_format_version: 1.0
  writer: codex-gpt-5
  what_changed: Added API-level coverage for DiffReport R6 audit endpoint (`GET /api/v1/reports/diff/{id}/audit`) including auth-required, success, and not-found behavior; executed full repository regression tests.
  commit: 31bf841
  commit_hash: 31bf841
  test_evidence: `python -m pytest tests/test_diff_audit_api.py -q` => 3 passed; `python -m pytest tests/ -q` => 124 passed
  next_step: rerun GitHub Actions UI smoke workflow and confirm green status after recent Playwright assertion updates.

- memory_type: manual-post-push
  record_format_version: 1.0
  writer: codex-gpt-5
  what_changed: Recorded memory hash-tracking commit for diff audit API tests.
  commit: 69888be
  commit_hash: 69888be
  test_evidence: governance pre-push hook accepted commit creation but blocked push until UTC-dated memory file existed
  next_step: push pending local commits after adding this UTC daily memory file.

- memory_type: manual-post-push
  record_format_version: 1.0
  writer: codex-gpt-5
  what_changed: Added stable `data-testid` hooks for governance UI (`gov-bar`, review button, advice signal value) and switched Playwright smoke assertions to testid-based checks to reduce wording-coupled CI failures.
  commit: c189245
  commit_hash: c189245
  test_evidence: static template check confirms all three testids exist in `static/index.html`
  next_step: push and rerun UI smoke workflow to verify green run under current wording.

- memory_type: manual-post-push
  record_format_version: 1.0
  writer: codex-gpt-5
  what_changed: Added `run_diff_audit` service-layer edge-case tests (not found, empty items, R6 violation detection) to lock diff audit behavior below API layer.
  commit: 88e6339
  commit_hash: 88e6339
  test_evidence: `python -m pytest tests/test_audit_service.py -q` => 3 passed; `python -m pytest tests/ -q` => 127 passed
  next_step: push test hardening commit and rerun UI smoke workflow in GitHub Actions.

- memory_type: manual-post-push
  record_format_version: 1.0
  writer: codex-gpt-5
  what_changed: Expanded JWT guard coverage to documents/classification/tables/data_source/disclosures routers and added dedicated API auth regression tests for each protected write endpoint.
  commit: 0a0fcc4
  commit_hash: 0a0fcc4
  test_evidence: `python -m pytest tests/test_auth_wiring_extended.py -q` => 5 passed; `python -m pytest tests/ -q` => 132 passed
  next_step: push auth expansion and rerun GitHub UI smoke to confirm end-to-end CI green.

- memory_type: manual-post-push
  record_format_version: 1.0
  writer: codex-gpt-5
  what_changed: Added workflow path filters to `.github/workflows/ui-smoke.yml` so UI smoke only runs when UI/test/workflow-related files change, reducing CI noise from memory-only commits.
  commit: pending
  commit_hash: pending
  test_evidence: workflow diff includes `paths` filters on both `pull_request` and `push` triggers
  next_step: push workflow update and confirm a future memory-only commit does not trigger UI smoke.
